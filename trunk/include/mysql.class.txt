/* function call diagram
* Funktions Aufruf Diagramm
* wenn kein Fehler (false) auftritt, geht es immer weiter
* -> funktionsaufrufe
* => funttionsrückgaben


SQL-> optimize("tablename");

 mysql::optimize("method", "tablename")
 	 -> checkup() 				// check:
	  		-> empty (tablename)	// * name not empty
	  			-> error()			// ->error, decide by ::DBG, what to do
	  			=> return false / die()
	  		=> return false
	  	=> return false
	  		-> empty($CONNECTION)	// * connection alive/existing
	  			-> error()
	  			=> return false / die()
	  		=> return false
	  	=> return false					//[* sql starts with right reserved word]
		on error
